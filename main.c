#include "main.h"

/**
 * main - entry point
 *
 * @argc: number of args
 * @argv: array of args
 *
 * Return: always 0
 */

int main(__attribute__((unused))int argc, char *argv[])
{
shell(argv[0]);
return (0);
}

